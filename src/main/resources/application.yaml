spring:
  application:
    name: mmrTest
  profiles:
    default: local

  messages:
    encoding: UTF-8

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

riot:
  api:
    key: ${RIOT_API_KEY:RGAPI-deb7af05-745c-4cb8-ace8-44e8ddd69294}

dev:
  allowlist:
    enabled: ${DEV_ALLOWLIST_ENABLED:false}
    riotIds: ${DEV_ALLOWLIST_RIOT_IDS:}

score:
  calibration-log-enabled: ${SCORE_CALIBRATION_LOG_ENABLED:true}
