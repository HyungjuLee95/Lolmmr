spring:
  application:
    name: mmrTest
  profiles:
    default: local

  messages:
    encoding: UTF-8

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

riot:
  api:
    key: ${RIOT_API_KEY:RGAPI-075b546c-a53b-4acd-b6a1-88404cc92c52}
 
dev:
  allowlist:
    enabled: ${DEV_ALLOWLIST_ENABLED:false}
    riotIds: ${DEV_ALLOWLIST_RIOT_IDS:}

score:
  calibration-log-enabled: ${SCORE_CALIBRATION_LOG_ENABLED:true}
